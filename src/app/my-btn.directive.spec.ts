import { MyBtnDirective } from './my-btn.directive';

describe('MyBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new MyBtnDirective();
    expect(directive).toBeTruthy();
  });
});
