import { ColormeDirective } from './colorme.directive';

describe('ColormeDirective', () => {
  it('should create an instance', () => {
    const directive = new ColormeDirective();
    expect(directive).toBeTruthy();
  });
});
